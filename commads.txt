# VIM SHORTCUTS
gg=G					# auto indent source code

# OTHERS
indent -kr -i8 -l1000 -sob		# beautify source code
chattr +i /etc/resolv.conf		# make file readonly
sudo blkid				# get uuid of harddisks
ssh-keygen -t rsa -C "your@email.com"	# setup ssh keys

